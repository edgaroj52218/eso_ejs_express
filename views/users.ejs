<!-- views/users.ejs -->
<%- include('partials/header') %>

<h2><%= message %></h2>
<ul>
    <% users.forEach(user => { %>
        <li><strong><%= user.name %></strong> - Age: <%= user.age %></li>
    <% }); %>
</ul>

<%- include('partials/footer') %>
